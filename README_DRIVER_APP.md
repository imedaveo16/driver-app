# تطبيق سائق الحماية المدنية (Driver App)

![Next.js](https://img.shields.io/badge/Next.js-15.3-black)
![TypeScript](https://img.shields.io/badge/TypeScript-5-blue)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-4-38bdf8)
![Firebase](https://img.shields.io/badge/Firestore-Real_Time-yellow)

تطبيق ويب متكامل للوحدات الميدانية في الحماية المدنية، مصمم للعمل على الأجهزة المحمولة مع واجهة عالية التباين للقيادة الليلية.

## 🚀 الميزات الرئيسية

### ✅ استقبال المهام في الوقت الحقيقي
- استماع دائم لـ Firebase Firestore
- تنبيهات داخل التطبيق للمهام الجديدة
- عرض تفاصيل المهمة: النوع، الأولوية، الوصف، الموقع

### ✅ الملاحة الذكية
- خريطة تفاعلية بوضع Dark Mode (Leaflet)
- عرض موقع الحادث وموقع السائق
- زر Google Maps للمسار المباشر
- حساب المسافة المتبقية

### ✅ تتبع الحالة
- شريط تقدم لمراحل التدخل:
  - قبول المهمة
  - في الطريق
  - وصل للموقع
  - العودة للمركز
  - اكتملت المهمة
- تحديث فوري لحالة البلاغ

### ✅ الموقع الحي
- تتبع موقع السائق كل 5 ثوانٍ
- تحديث الموقع في Firestore
- عرض المسافة المتبقية

### ✅ واجهة مستخدم محسنة
- تصميم عالي التباين للقيادة الليلية
- ألوان برتقالية/حمراء على خلفية سوداء
- تصميم متجاوب للأجهزة المحمولة
- دعم كامل للعربية RTL

## 🛠 التقنيات المستخدمة

| التقنية | الاستخدام |
|---------|-----------|
| Next.js 15 | إطار العمل الرئيسي |
| TypeScript 5 | لغة البرمجة |
| Tailwind CSS 4 | التنسيق |
| shadcn/ui | مكونات الواجهة |
| Firebase Firestore | قاعدة البيانات الحقيقية |
| Leaflet.js | الخرائط |
| Geolocation API | تتبع الموقع |

## 📋 المتطلبات

- Node.js 18+ أو Bun
- حساب Firebase
- متصفح حديث مع دعم Geolocation

## 🔧 التثبيت والتشغيل

### 1. استنساخ المشروع

```bash
git clone <your-repo-url>
cd my-project
```

### 2. تثبيت الحزم

```bash
bun install
# أو
npm install
```

### 3. إعداد Firebase

1. أنشئ مشروع في [Firebase Console](https://console.firebase.google.com/)
2. فعل Firestore Database
3. أنشئ ملف `.env.local`:

```bash
cp .env.example .env.local
```

4. عدّل `.env.local` ببيانات Firebase الخاصة بك:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
```

### 5. تشغيل التطبيق

```bash
bun run dev
```

افتح المتصفح على: `http://localhost:3000`

## 📱 الاستخدام

### دخول السائق

1. اختر "تطبيق السائق" من الصفحة الرئيسية
2. أدخل البريد الإلكتروني ورقم تعريف السائق
3. سيتم حفظ البيانات تلقائياً

### استقبال المهام

عند وصول مهمة جديدة:
- يظهر تنبيه منبثق مع تفاصيل المهمة
- عداد تنازلي 30 ثانية للقبول أو الرفض
- صوت تنبيه عند وصول مهمة

### تحديث الحالة

استخدم شريط التقدم لتحديث حالة التدخل:
1. **قبول المهمة** ✓ - عند استلام المهمة
2. **في الطريق** 🚚 - عند بدء التحرك
3. **وصل للموقع** 📍 - عند الوصول
4. **العودة للمركز** ↩️ - عند الإنتهاء
5. **اكتملت** ✅ - عند الوصول للمركز

### الملاحة

**استخدام الخريطة الداخلية:**
- استعرض الموقع على الخريطة
- رؤية المسافة المتبقية

**استخدام Google Maps:**
- اضغط زر "Google Maps"
- سيفتح المسار في تطبيق Google Maps

## 🔗 التكامل مع منصة التحكم (C.C.O)

### إرسال مهمة من المنصة

```javascript
await setDoc(doc(db, 'reports', missionId), {
  type: 'emergency',
  priority: 'high',
  description: 'حادث مروري طارئ',
  location: {
    lat: 36.7538,
    lng: 3.0588,
    address: 'شارع الاستقلال'
  },
  status: 'pending',
  assignedDriverId: 'driver_123',
  createdAt: new Date(),
  updatedAt: new Date(),
});
```

### مراقبة موقع السائق

```javascript
onSnapshot(doc(db, 'drivers', driverId), (doc) => {
  const driver = doc.data();
  console.log('Location:', driver.currentLocation);
});
```

## 📂 هيكل المشروع

```
src/
├── app/
│   ├── page.tsx                    # الصفحة الرئيسية
│   ├── driver/
│   │   ├── page.tsx                # لوحة تحكم السائق
│   │   └── login/
│   │       └── page.tsx            # صفحة تسجيل الدخول
│   └── globals.css                 # التنسيقات
├── components/
│   ├── driver/
│   │   ├── mission-map.tsx         # الخريطة
│   │   ├── mission-alert.tsx       # التنبيه
│   │   └── mission-status-tracker.tsx  # شريط الحالة
│   └── ui/                         # مكونات shadcn/ui
├── hooks/
│   ├── use-driver-location.ts      # تتبع الموقع
│   └── use-toast.ts                # الإشعارات
└── lib/
    ├── firebase/
    │   ├── config.ts               # إعدادات Firebase
    │   └── firestore.ts            # دوال Firestore
    └── utils.ts                    # دوال مساعدة
```

## 🔄 التحويل لتطبيق Android (APK)

### باستخدام Capacitor (موصى به)

```bash
# تثبيت Capacitor
npm install @capacitor/core @capacitor/cli @capacitor/android

# إعداد التطبيق
npx cap init CivilProtectionDriver com.civilprotection.driver

# إضافة Android
npx cap add android

# البناء والمزامنة
npm run build
npx cap sync android

# فتح Android Studio
npx cap open android
```

في Android Studio:
1. اضغط Build → Build Bundle(s)/APK(s)
2. اختر Build APK(s)
3. سيتم إنشاء `Driver_C2_Command.apk`

### PWA (بدون تحويل)

جعل التطبيق PWA للتحميل على الهواتف:

```bash
npm install next-pwa
```

## 🔒 أمان Firestore

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /reports/{reportId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }

    match /drivers/{driverId} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == driverId;
    }
  }
}
```

## 🐛 استكشاف الأخطاء

### لا يظهر الموقع
- تأكد من منح إذن الموقع في المتصفح
- تحقق من إعدادات GPS
- استخدم HTTPS في الإنتاج

### لا تظهر المهام
- تأكد من `assignedDriverId` في المهمة
- تحقق من Firestore rules
- تأكد من الاتصال بالإنترنت

### الخريطة لا تُحمل
- تحقق من الاتصال بالإنترنت
- تأكد من Leaflet CSS و JS مُحمّلان
- تأكد من ارتفاع container

## 📖 التوثيق

- [دليل الاستخدام الكامل](./DRIVER_APP_GUIDE.md)
- [Next.js Documentation](https://nextjs.org/docs)
- [Firebase Documentation](https://firebase.google.com/docs)
- [Leaflet Documentation](https://leafletjs.com/reference.html)

## ⚠️ ملاحظات هامة

1. **بيئة التطوير**: يستخدم HTTP
2. **بيئة الإنتاج**: يتطلب HTTPS لتفعيل Geolocation
3. **Firebase**: تأكد من إعداد security rules
4. **APK**: هذا تطبيق ويب، استخدم Capacitor للتحويل

## 📞 الدعم

للدعم والاستفسارات، يرجى مراجعة [دليل الاستخدام](./DRIVER_APP_GUIDE.md)

## 📄 الترخيص

الحماية المدنية الجزائرية © 2024

---

**مطور بواسطة Z.ai Code**
